kind = "web"
previewPath = "/"
title = "Upsell"
version = "1.0.0"
id = "artifacts/upsell"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19555

[services.development]
run = "pnpm --filter @workspace/upsell run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/upsell", "run", "build" ]
publicDir = "artifacts/upsell/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19555"
BASE_PATH = "/"
